<h1>Breweries</h1>

<p>Number of breweries: <%= @active_breweries.count %></p>

<div ng-controller="BreweriesController">
  <p><input type="text" class="form-control" id="brewerySearch" placeholder="Search" ng-model="searchText"></p>
  <table class="table table-hover">
    <thead>
    <th> <a ng-click="orderClick('name')">Name</a> </th>
    <th> <a ng-click="orderClick('year')">Year</a> </th>
    <th> <a ng-click="orderClick('beers.count')">Beers</a> </th>
    </thead>
    <tr ng-repeat="brewery in breweries | orderBy:activeOrder | filter:searchText | filter:'true'">
      <td> <a ng-href="breweries/{{brewery.id}}"> {{brewery.name}} </a> </td>
      <td>{{brewery.year}}</td>
      <td>{{brewery.beers.count}}</td>
    </tr>
  </table>
</div>

<br>
<%= link_to 'New Brewery', new_brewery_path if current_user %>
<br>
<%= link_to "List of beers", beers_path %>
