<h2>Create new rating</h2>

<%= bootstrap_form_for(@rating) do |f| %>
        <%= f.collection_select :beer_id, @beers, :id, :to_s %>
        <%= f.number_field :score, placeholder: "Score", hide_label: :true %>
    <%= f.submit "Create Rating", class: "btn btn-primary" %>
<% end %>
