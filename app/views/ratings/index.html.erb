<h2>Ratings</h2>

<p>Total ratings: <%= @ratings.count %> </p>

<b>The most recent ratings:</b>
<ul>
  <% @ratings.recent.each do |rating| %>
      <li> <%= link_to rating.beer.name, rating.beer %>  by <%= link_to rating.user.username, rating.user %> </li>
  <%end%>
</ul>

<b>Top 3 breweries by rating:</b>
<ul>
  <% @topBreweries.each do |b| %>
      <li> <%= link_to b.name, b %> <%= b.average_rating %> </li>
  <% end %>
</ul>

<b>Top 3 beers by rating:</b>
<ul>
  <% @topBeers.each do |b| %>
      <li> <%= link_to b.name, b %> <%= b.average_rating %> </li>
  <% end %>
</ul>

<p>
  <% unless current_user.nil? %>
      <%= link_to('Add a new rating', new_rating_path) %>
  <% end %>
</p>